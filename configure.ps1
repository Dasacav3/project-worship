# Npm packages for backend && Npm packages for frontend && Compile tailwindcss and postcss && Go root folder
npm install -g postcss-cli postcss && npm install && cd client && npm install && npx postcss src/assets/index.css -o src/assets/main.css && Set-Location ..
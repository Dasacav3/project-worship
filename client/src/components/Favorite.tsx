import { useState, useEffect } from 'react';

const Favorite = (props: Props) => {
    return (
        <>
        </>
    );
};

interface Props {
    icon: string;
    marked: boolean;
}

export default Favorite;
